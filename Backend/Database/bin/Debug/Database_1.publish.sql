/*
Deployment script for HRMSPro

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "HRMSPro"
:setvar DefaultFilePrefix "HRMSPro"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[UserRole].[CreateByDateTime] is being dropped, data loss could occur.

The column [dbo].[UserRole].[CreateByUserId] is being dropped, data loss could occur.

The column [dbo].[UserRole].[CreatedByDateTime] on table [dbo].[UserRole] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[UserRole].[CreatedByUserId] on table [dbo].[UserRole] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[UserRole].[UserId] on table [dbo].[UserRole] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[UserRole])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping Foreign Key [dbo].[FK_User_Role]...';


GO
ALTER TABLE [dbo].[UserRole] DROP CONSTRAINT [FK_User_Role];


GO
PRINT N'Dropping Foreign Key [dbo].[FK_Employee_UserId]...';


GO
ALTER TABLE [dbo].[Employee] DROP CONSTRAINT [FK_Employee_UserId];


GO
PRINT N'Starting rebuilding table [dbo].[UserRole]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_UserRole] (
    [Id]                UNIQUEIDENTIFIER NOT NULL,
    [RoleId]            UNIQUEIDENTIFIER NOT NULL,
    [UserId]            UNIQUEIDENTIFIER NOT NULL,
    [CreatedByUserId]   UNIQUEIDENTIFIER NOT NULL,
    [CreatedByDateTime] DATETIME         NOT NULL,
    [UpdatedByUserId]   UNIQUEIDENTIFIER NULL,
    [UpdatedDateTime]   DATETIME         NULL,
    [InActiveDateTime]  DATETIME         NULL,
    CONSTRAINT [tmp_ms_xx_constraint_PK_UserRole1] PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[UserRole])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_UserRole] ([Id], [RoleId], [UpdatedByUserId], [UpdatedDateTime], [InActiveDateTime])
        SELECT   [Id],
                 [RoleId],
                 [UpdatedByUserId],
                 [UpdatedDateTime],
                 [InActiveDateTime]
        FROM     [dbo].[UserRole]
        ORDER BY [Id] ASC;
    END

DROP TABLE [dbo].[UserRole];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_UserRole]', N'UserRole';

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_constraint_PK_UserRole1]', N'PK_UserRole', N'OBJECT';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating Table [dbo].[Users]...';


GO
CREATE TABLE [dbo].[Users] (
    [Id]               UNIQUEIDENTIFIER NOT NULL,
    [FirstName]        NVARCHAR (150)   NOT NULL,
    [LastName]         NVARCHAR (150)   NULL,
    [UserName]         NVARCHAR (150)   NOT NULL,
    [Email]            NVARCHAR (250)   NOT NULL,
    [PasswordHash]     NVARCHAR (500)   NOT NULL,
    [CreatedDateTime]  DATETIME         NOT NULL,
    [CreatedByUserId]  UNIQUEIDENTIFIER NOT NULL,
    [UpdatedDateTime]  DATETIME         NULL,
    [UpdatedByUserId]  UNIQUEIDENTIFIER NULL,
    [IsActive]         BIT              NOT NULL,
    [InActiveDateTime] DATETIME         NULL,
    CONSTRAINT [PK_Users] PRIMARY KEY CLUSTERED ([Id] ASC)
);


GO
PRINT N'Creating Foreign Key [dbo].[FK_User_Role]...';


GO
ALTER TABLE [dbo].[UserRole] WITH NOCHECK
    ADD CONSTRAINT [FK_User_Role] FOREIGN KEY ([RoleId]) REFERENCES [dbo].[Role] ([Id]);


GO
PRINT N'Creating Foreign Key [dbo].[FK_Role_UserId]...';


GO
ALTER TABLE [dbo].[UserRole] WITH NOCHECK
    ADD CONSTRAINT [FK_Role_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[Users] ([Id]);


GO
PRINT N'Creating Foreign Key [dbo].[FK_Employee_UserId]...';


GO
ALTER TABLE [dbo].[Employee] WITH NOCHECK
    ADD CONSTRAINT [FK_Employee_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[Users] ([Id]);


GO
PRINT N'Creating Procedure [dbo].[GetUserDetailsWithUsername]...';


GO
CREATE PROCEDURE [dbo].[GetUserDetailsWithUsername]
(
	@UserName NVarchar(max) = null
)
AS
BEGIN
	SELECT Id,UserName,Email,PasswordHash FROM [Users] WITH (NOLOCK) WHERE UserName = @UserName AND IsActive = 1
END
GO
PRINT N'Creating Procedure [dbo].[UpsertUser]...';


GO
CREATE PROCEDURE [dbo].[UpsertUser]
(
    @Id UNIQUEIDENTIFIER = NULL, 
    @FirstName NVARCHAR(150) = NULL,
    @LastName NVARCHAR(150) = NULL,
    @UserName NVARCHAR(150) = NULL,
    @Email NVARCHAR(250) = NULL,
    @PasswordHash NVARCHAR(500) = NULL,
    @RoleId UNIQUEIDENTIFIER = NULL
)
AS
BEGIN
    SET NOCOUNT ON;

    -- If Id is NULL, generate new one
    IF @Id IS NULL
        SET @Id = NEWID();

    DECLARE @OperationPerformedBy UNIQUEIDENTIFIER = (SELECT TOP 1 Id From [Users] WHERE UserName LIKE '%Support%') ;

    MERGE INTO Users AS TARGET
    USING (
        SELECT
            @Id AS Id,
            @FirstName AS FirstName,
            @LastName AS LastName,
            @UserName AS UserName,
            @Email AS Email,
            @PasswordHash AS PasswordHash 
    ) AS SOURCE
    ON TARGET.Id = SOURCE.Id

    WHEN MATCHED THEN
        UPDATE SET
            TARGET.FirstName = SOURCE.FirstName,
            TARGET.LastName = SOURCE.LastName,
            TARGET.UserName = SOURCE.UserName,
            TARGET.Email = SOURCE.Email,
            TARGET.PasswordHash = SOURCE.PasswordHash,
            TARGET.UpdatedDateTime = GETUTCDATE(),
            TARGET.UpdatedByUserId = @OperationPerformedBy

    WHEN NOT MATCHED THEN
        INSERT (Id, FirstName, LastName, UserName, Email, PasswordHash,CreatedByUserId,CreatedDateTime)
        VALUES (
            SOURCE.Id,
            SOURCE.FirstName,
            SOURCE.LastName,
            SOURCE.UserName,
            SOURCE.Email,
            SOURCE.PasswordHash,
            @OperationPerformedBy,
            GETUTCDATE()
        );


   MERGE INTO UserRole AS TARGET
    USING (
        SELECT 
            @Id AS UserId,
            @RoleId AS RoleId
    ) AS SOURCE
    ON TARGET.UserId = SOURCE.UserId

    WHEN MATCHED THEN
        UPDATE SET 
            TARGET.RoleId = SOURCE.RoleId,
            TARGET.UpdatedDateTime = GETUTCDATE(),
            TARGET.UpdatedByUserId = @OperationPerformedBy

    WHEN NOT MATCHED THEN
        INSERT (
            Id,
            UserId,
            RoleId,
            [CreatedByUserId],
            [CreatedByDateTime]
        )
        VALUES (
            NEWID(),
            SOURCE.UserId,
            SOURCE.RoleId,
            @OperationPerformedBy,
            GETUTCDATE()
        );



END
GO
/*
Post-Deployment Script Template							
--------------------------------------------------------------------------------------
 This file contains SQL statements that will be appended to the build script.		
 Use SQLCMD syntax to include a file in the post-deployment script.			
 Example:      :r .\myfile.sql								
 Use SQLCMD syntax to reference a variable in the post-deployment script.		
 Example:      :setvar TableName MyTable							
               SELECT * FROM [$(TableName)]					
--------------------------------------------------------------------------------------
*/

DECLARE @DefaultUserId UNIQUEIDENTIFIER =  NULL
SET @DefaultUserId = (SELECT TOP 1 Id From [Users] WHERE UserName LIKE '%Support%') 
IF(@DefaultUserId IS NULL)
BEGIN
   SET @DefaultUserId = NEWID()

	MERGE INTO [Users] AS TARGET
	USING 
	(VALUES
	(@DefaultUserId, 'Suport','','Support','Support@gmail.com','Test123!',1)
	)
	AS SOURCE
	( Id,FirstName,LastName,UserName,Email,PasswordHash,IsActive)
	ON TARGET.UserName = SOURCE.UserName
	WHEN MATCHED 
	THEN 
	UPDATE
		SET FirstName = SOURCE.FirstName,
			LastName = SOURCE.LastName,
			UserName = SOURCE.UserName,
			Email = SOURCE.Email,
			PasswordHash = SOURCE.PasswordHash,
			IsActive = SOURCE.IsActive,
			[UpdatedDateTime] = GETUTCDATE(),
			[UpdatedByUserId] = @DefaultUserId
	WHEN NOT MATCHED 
	THEN
	INSERT 
	( Id,FirstName,LastName,UserName,Email,PasswordHash,IsActive,[CreatedDateTime],[CreatedByUserId])
	VALUES(SOURCE.[Id],SOURCE.FirstName,SOURCE.LastName,SOURCE.UserName,SOURCE.Email,SOURCE.PasswordHash,SOURCE.IsActive,GETUTCDATE(),@DefaultUserId);
END

 

MERGE INTO [Role] AS TARGET
USING 
(VALUES
(NEWID(), 'Super Admin'),
(NEWID(), 'User')
)
AS SOURCE
([Id],[RoleName])
ON TARGET.[RoleName] = SOURCE.[RoleName]
WHEN MATCHED 
THEN 
UPDATE
	SET [RoleName] = SOURCE.[RoleName],
		[UpdatedDateTime] = GETUTCDATE(),
		[UpdatedByUserId] = @DefaultUserId
WHEN NOT MATCHED 
THEN
INSERT 
([Id],[RoleName],[CreatedDateTime],[CreatedByUserId])
VALUES(SOURCE.[Id],SOURCE.[RoleName],GETUTCDATE(),@DefaultUserId);


MERGE INTO Department AS TARGET
USING 
(VALUES
(NEWID(), 'HR'),
(NEWID(), 'IT'),
(NEWID(), 'Finance')
)
AS SOURCE
([Id],[DepartmentName])
ON TARGET.[DepartmentName] = SOURCE.[DepartmentName]
WHEN MATCHED 
THEN 
UPDATE
	SET [DepartmentName] = SOURCE.[DepartmentName],
		[UpdatedDateTime] = GETUTCDATE(),
		[UpdatedByUserId] = @DefaultUserId
WHEN NOT MATCHED 
THEN
INSERT 
([Id],[DepartmentName],[CreatedDateTime],[CreatedByUserId])
VALUES(SOURCE.[Id],SOURCE.[DepartmentName],GETUTCDATE(),@DefaultUserId);



MERGE INTO [Designation] AS TARGET
USING 
(
VALUES
(NEWID(), 'Senior Engineer'),
(NEWID(), 'HR Manager'),
(NEWID(), 'Accountant') 
)
AS SOURCE
([Id],[DesignationName])
ON TARGET.[DesignationName] = SOURCE.[DesignationName]
WHEN MATCHED 
THEN 
UPDATE
	SET [DesignationName] = SOURCE.[DesignationName],
		[UpdatedDateTime] = GETUTCDATE(),
		[UpdatedByUserId] = @DefaultUserId
WHEN NOT MATCHED 
THEN
INSERT 
([Id],[DesignationName],[CreatedDateTime],[CreatedByUserId])
VALUES(SOURCE.[Id],SOURCE.[DesignationName],GETUTCDATE(),@DefaultUserId);
GO

GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
ALTER TABLE [dbo].[UserRole] WITH CHECK CHECK CONSTRAINT [FK_User_Role];

ALTER TABLE [dbo].[UserRole] WITH CHECK CHECK CONSTRAINT [FK_Role_UserId];

ALTER TABLE [dbo].[Employee] WITH CHECK CHECK CONSTRAINT [FK_Employee_UserId];


GO
PRINT N'Update complete.';


GO
